# 目录清理记录

清理日期：2026-08-08

## 最终状态

项目最终保留 1051 个文件、71 个目录，共 236.760 MiB。扫描未发现 `tmp/`、`.pytest_cache/`、
`__pycache__/` 或空白占位目录。体积主要来自 Q4 的 500 个正式分片、完整整数域计数矩阵、
冻结结果、FreeCAD 模型、两份论文 PDF、24 份提交源码及白名单支撑材料。

论文目录只保留两份用途明确的 PDF：

- `论文/A题论文_当前审阅稿_LaTeX.pdf`：184 页完整稿，含 24 个真实程序；
- `论文/A题论文_正文便阅稿_LaTeX.pdf`：21 页正文便阅稿，省略源码附录。

## 本轮移除

以下可再生产物已送入 Windows 回收站，可恢复：

- 旧的 189 页 `A题_微构体导电仿真优化_INTERNAL_QA.pdf`；
- 完整稿和便阅稿的 `latex_internal*` 辅助构建目录；
- 184 页逐页渲染、关键页 PNG、文本抽取、灰度检查和两次重建前备份；
- `tests/`、`公共代码/`、`论文/src/` 下的 Python 缓存。

重建前备份只在新 `提交源码/`、`支撑材料内容/`、PDF 和测试全部通过后移除。正式结果、源码、
图件、模型、审计和哈希清单均未删除。

## 保留边界

- `问题/问题4/results/D_screen2000_confirm50000/` 整棵保留，包括 500 个正式分片、合并结果、
  日志、完整整数域矩阵、冻结配置和独立审计；
- `提交源码/` 保留 24 个带 AI 注释头的冻结副本及 `source-manifest.json`；
- `支撑材料内容/` 保留匿名化结果、压缩样本、正式图件、FreeCAD 模型和 `SHA256SUMS.txt`；
- `论文/generated/` 只保留正文中间件、构建清单、结果宏与源码附录输入；LaTeX 编译目录可按需重建；
- `00_赛题与附件/`、`01_资料检索/原始文献/` 与官方模板均只读保留。

## 复验

- 全套测试：`211 passed, 2 skipped, 7 subtests passed`；
- 最终论文与支撑材料相关测试：`63 passed`；
- 工作流 doctor：Python、XeLaTeX、latexmk、Poppler、WinRAR 及核心科学计算包均可用；
- 工作流 audit：`0 error`。其剩余提示主要来自通用审计器不展开 `generated/content.tex`，以及
  支撑材料按规则复制冻结结果/图件产生的预期重复；正式打包器使用白名单，不收录构建缓存。

## 正式提交阻塞项

实际参赛组别和 `CM` 加 7 位数字编号尚未提供，因此没有生成正式命名 PDF 与附件压缩包，
也没有填写承诺书身份信息。这些信息不得虚构。
