# Q4 完成前交付预检

审计日期：2026-08-08

审计边界：只检查不依赖 Q4 最终数值的 LaTeX、源码附录、匿名化、支撑材料与压缩包门禁；未修改 `问题/问题4/src/solve.py`、`公共代码/mixed_microstructure_sim.py` 或几何核，两个交付配置仍保持 `pending_q4`，未生成假正式包。

## 1. 已修复的非 Q4 阻断

1. `prepare_submission_sources.py` 原先要求 Q1 恰好 4 项受控替换，而当前冻结候选白名单实际有 7 项；状态转为 `frozen` 后必然失败。现已统一为 7，并加入“临时冻结副本”回归测试。
2. 正式 LaTeX 源码附录原先只读取原源码和白名单，却声称已与 `提交源码/source-manifest.json` 对账。现 final 分支必须逐项校验白名单哈希、路径与顺序、标题、原源码/提交副本哈希和字节数、两行 AI 注释头及用户主目录路径，并直接使用冻结提交副本生成附录。
3. 显式 `--review` internal 便阅路径同时移除“附录 C：主要程序”标题和源码输入，并以小号附录排下完整 A/B 内容；完整 internal/final 附录不受影响。当前审阅件 `论文/A题论文_当前审阅稿_LaTeX.pdf` 为 11 页，SHA-256 为 `A1907F1A587A29CEF2DDC011E56DB253DA0C0267D865BF0F1F740ACB00543A8E`。
4. FreeCAD 匿名门禁由只检查 `Document.xml` 扩展为扫描 FCStd 容器全部成员，再检查 `Company`、`CreatedBy`、`LastModifiedBy` 为空。
5. 支撑材料测试不再把 `pending_q4` 写死；同一套测试在合法的 `pending_q4` 和 `frozen` 生命周期状态下都能运行，未冻结拒绝写出的检查改用临时配置。

## 2. 门禁结果

- 相关回归测试：`26 passed`，覆盖支撑包和 LaTeX 链。
- 实际 final 命令在当前状态正确拒绝，原因为 `Q4 final_evidence_available` 尚未冻结为 `true`；没有产生正式 PDF。
- 两条准备 CLI 均正确拒绝 `pending_q4`，项目根目录没有 `提交源码/` 或 `支撑材料内容/`。
- 匿名实物扫描：14 个现有支撑文本/JSON、4 个正式 FCStd、23 个源码副本候选通过；论文源与生成 TeX 未检出用户主目录路径。当前仅缺 6 个预期 Q4 完成文件。
- 20 MiB 门禁同时具备原子暂存、精确成员表、ZIP CRC、路径安全、SHA-256 树和超限后清理测试。

容量预检使用当时连续完成的 14400 个 Q4 trial 只读重建非正式完整域矩阵，仅估计压缩率，不作为 Q4 结果。临时容量测试原件已清理，保留的汇总数值如下：

- 当前文件、源码、Q2/Q3 压缩样本及部分 Q4 矩阵的测试 ZIP：9367775 bytes；
- 部分矩阵 NPZ：2542557 bytes；
- 按“再增加 1 份部分矩阵体积 + 3 MiB 余量”投影：15056060 bytes；
- 相对 20971520-byte 门禁余量：5915460 bytes。

最终仍必须以真实 50000 次矩阵生成后的 ZIP 实测为准；脚本会对超过 20 MiB 的结果硬拒绝。

## 3. 待 Q4 生成的六项文件

1. `问题/问题4/results/D_screen2000_confirm50000/q4_confirmation.json`
2. `问题/问题4/results/D_screen2000_confirm50000/q4_confirmation.csv`
3. `问题/问题4/results/D_screen2000_confirm50000/q4_confirmation_integer_domain_counts.npz`
4. `问题/问题4/results/D_screen2000_confirm50000/q4_confirmation_integer_domain_analysis.json`
5. `论文/figures/generated/q4_cost_frontier.png`
6. `论文/figures/generated/q4_cost_frontier.audit.json`

## 4. Pending 反馈分类

| 反馈 ID | 分类 | 当前处理 | 关闭条件 |
|---|---|---|---|
| `FB-20260807-212312-472503` | project / 最终模型口径 | 已落实到独立片段、D 边界和独立随机介质实现；暂不提前关闭 | Q4 最终审计、结果注册表和正式 PDF 三方一致 |
| `FB-20260807-223922-960812` | project / 论文过程叙事 | 与下面两项属于同一正式稿门禁；保留原记录 | final `scan_final_sources` 与 PDF 文本复查均无修改、澄清、旧方案或推翻过程叙事 |
| `FB-20260807-230601-706122` | project / 论文过程叙事 | 同上 | 同上 |
| `FB-20260807-231431-604948` | project / 论文过程叙事 | 同上 | 同上 |
| `FB-20260808-033242-734023` | project / LaTeX 权威交付 | internal 和 11 页便阅链已成立，正式状态仍 pending | 生成并逐页复核 `A<CM编号>.pdf`，记录最终 SHA-256，且全部提交物位于项目目录 |

## 5. Q4 完成后的最短命令链

先确认正在运行的 `--stage confirm --resume --register-results` 正常退出，再执行：

```powershell
Set-Location "C:\Users\23258\Desktop\华数杯\A题_微构体导电仿真优化"
python 问题/问题4/src/audit_confirmation.py
python 论文/figures/src/build_q4_frontier.py `
  --screening 问题/问题4/results/D_screen2000_confirm50000/q4_screening.json `
  --final 问题/问题4/results/D_screen2000_confirm50000/q4_summary.json
python -m pytest -q
```

核对上述六项文件、独立审计、结果注册表和三维资产后，只把 `支撑材料配置.json` 与 `论文/src/source_appendix_allowlist.json` 的 `status` 从 `pending_q4` 改为 `frozen`。随后用实际组别和实际编号执行：

```powershell
$Group = "<研究生|本科生|专科生>"
$Team = "CM<7位数字>"
python 论文/src/prepare_submission_sources.py
python 论文/src/build_latex.py --mode final --group $Group --competition-id $Team
python 论文/src/prepare_support_materials.py --team-id $Team
python -m pytest -q
Get-FileHash -Algorithm SHA256 "论文/A${Team}.pdf", "A${Team}附件.zip"
```

正式 PDF 和附件 ZIP 的脚本均带匿名、命名、哈希及完整性门禁；任何一步失败都不得手工绕过。
