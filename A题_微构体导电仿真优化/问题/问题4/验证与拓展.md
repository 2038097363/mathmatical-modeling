# 问题四验证与拓展

## 已完成验证

1. 成功次数矩阵形状为 $620\times5484$，数值范围为 $[0,50000]$。
2. 沿 $N_A$ 和 $N_B$ 两个方向的单调性违例均为 0。
3. 随机抽取 63 个矩阵单元，逐 trial 直接查询 Pareto 前沿，结果与矩阵完全一致。
4. 经验最低点 $(612,12)$ 的成功数由矩阵直接读得为 45000；所有严格更低成本正整数设计均低于该成功数门槛。
5. $(616,1)$ 在 Q4 流中的 619 项校正下界为 $90.0086\%$；Q3 独立流通过单调性给出另一条可行证书。
6. FreeCAD 模型绑定 trial 000001，覆盖 616 个 A 源、1 个 B 源和 1095 个实际片段；恢复的见证含 10 个片段、11 条接触边、0 条同源内部边。

## 适用边界

结论以题给均匀独立随机生成、$1.8\ \mathrm{nm}$ 接触阈值和 D 边界截断合同为前提。A 的边界
几何使用中心线切段近似；半径侧缘越界与斜切面的完整布尔重建未进入主仿真。若制备过程存在
取向、团聚或排斥，应重新标定点过程并复算概率前沿。
